{
  "name": "retro-assistant-widget",
  "version": "1.0.0",
  "description": "Retro floating desktop widget for Claude Code, Antigravity, Codex, and OpenCode",
  "main": "main.js",
  "scripts": {
    "start": "electron .",
    "build": "electron-packager . RetroAssistWidget --platform=win32 --arch=x64 --overwrite --out=dist"
  },
  "devDependencies": {
    "electron": "^30.0.1",
    "electron-builder": "^24.13.3",
    "electron-packager": "^17.1.2"
  }
}
